name: Build images

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 5'

env:
  KERNEL_BRANCH: 6.12.1

jobs:
  build-image:
    runs-on: ubuntu-latest

    container:
      image: alpine:3.18

    steps:
      - name: Install dependencies (Alpine 3.18)
        run: |
          apk update
          apk add --no-cache \
            bash \
            git \
            sudo \
            build-base \
            bc \
            bison \
            flex \
            openssl-dev \
            elfutils-dev \
            perl \
            python3 \
            py3-pip \
            rsync \
            tar \
            xz \
            curl \
            findutils \
            grep \
            coreutils \
            util-linux \
            linux-headers

      - name: Check out build configurations
        uses: actions/checkout@v4

      - name: Substitute placeholders in configs
        shell: sh
        run: |
          find . -type f -name "*.cfg" -exec sed -i "s|HOME|$HOME|;s|NPROC|$(nproc)|" {} +

      - name: Install pmbootstrap from git
        run: |
          git clone --depth=1 https://gitlab.postmarketos.org/postmarketOS/pmbootstrap.git
          mkdir -p ~/.local/bin
          ln -s "$PWD/pmbootstrap/pmbootstrap.py" ~/.local/bin/pmbootstrap
          chmod +x ~/.local/bin/pmbootstrap
          export PATH="$HOME/.local/bin:$PATH"
          pmbootstrap --version

      - name: Initialize pmbootstrap
        run: |
          echo -e '\n\n' | pmbootstrap init || true

      - name: Set up pmaports
        run: |
          cd ~/.local/var/pmbootstrap/cache_git/pmaports
          git remote add mainlining https://github.com/mainlining/pmaports.git
          git fetch mainlining nikroks/alioth
          git reset --hard mainlining/nikroks/alioth

      - name: Clone kernel sources
        run: |
          git clone https://github.com/mainlining/linux.git \
            --single-branch \
            --branch nikroks/alioth \
            --depth 1 ./linux

      - name: Choose device config
        run: |
          mkdir -p ~/.config
          cp xiaomi-alioth.cfg ~/.config/pmbootstrap_v3.cfg

      - name: Compile kernel with envkernel
        shell: bash
        run: |
          cd linux
          source ../pmbootstrap/helpers/envkernel.sh
          make defconfig sm8250.config
          make -j$(nproc)
          pmbootstrap build --envkernel linux-postmarketos-qcom-sm8250-alioth

      - name: Create artifact directory
        run: mkdir out

      - name: Build xiaomi-alioth image
        run: |
          cp xiaomi-alioth.cfg ~/.config/pmbootstrap_v3.cfg
          pmbootstrap install --password 147147
          pmbootstrap export
          cp /tmp/postmarketOS-export/boot.img out/boot-xiaomi-alioth.img
          cp /tmp/postmarketOS-export/xiaomi-alioth.img out/rootfs-xiaomi-alioth.img

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xiaomi-alioth
          path: out/*-alioth.img*
          retention-days: 7
